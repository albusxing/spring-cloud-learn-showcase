spring:
  cloud:
    config:
      # 指定 ConfigServer 的 url
      uri: http://configserver.com:9999
      # 指定远程仓库的分支
      label: master
      # 指定从远程仓库拉取的配置文件
      name: application-consumer-config
      # 指定环境
      profile: dev